node_modules
dist
build

# Env files: ignore everything except the committed placeholder template.
.env
.env.*
!.env.example

*.log
.DS_Store

# One-time exam-build keys (never commit)
EXAM_KEYS.txt

# intermediate exam section cache
public/exams/_parts/
